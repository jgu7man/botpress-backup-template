import { workflow } from "./workflow.state";
// Node: ExistsValidation - nd-f2ef953fb1
// Resetting the answer type in a workflow. - ins-f3fc8fabd8

// ------------------ EXECUTE CODE -------------------------

if(workflow.answerType) {
    workflow.answerType = ''
}

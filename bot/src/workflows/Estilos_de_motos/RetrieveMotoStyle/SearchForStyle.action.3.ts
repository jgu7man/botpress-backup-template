import { workflow } from "./workflow.state";
// Node: SearchForStyle - nd-3887953802
// Calculate Length of Moto List in Workflow Object - ins-3fff61566e

export {};

// ------------------ EXECUTE CODE -------------------------

workflow.motoListLength = workflow.motoList?.length ?? 0

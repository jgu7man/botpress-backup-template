// const { formStatus } = conversation ?? {};
// Object.entries(formStatus!.formValue).forEach(([fieldName, field], index) => {
//   if (field.status === "PENDING") {
//     conversation.formStatus = {
//       currentField: field.property_name,
//       nextField: FormDefinitionTable[index + 1]?.property_name || ""
//     };
//   }
// });
